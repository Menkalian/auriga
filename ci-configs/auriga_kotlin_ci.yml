kotlinPluginBuild:
  stage: processor-build
  script:
    - cd auriga-kotlin-plugin
    - gradle publishToMavenLocal
  artifacts:
    paths:
      - auriga-kotlin-plugin/build
    expire_in: 1 week

kotlinPluginTest:
  stage: processor-test
  script:
    - cd auriga-kotlin-plugin
    - gradle test

kotlinPluginPublish:
  stage: processor-deploy
  only:
    - main
  script:
    - cd auriga-kotlin-plugin
    - gradle publish
  artifacts:
    paths:
      - auriga-kotlin-plugin/build/*.jar
    expire_in: 1 month